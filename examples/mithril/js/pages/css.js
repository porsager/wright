import m from 'mithril'

export default {
  view: () => [
    m('h1', 'css'),
    m('p', 'So this ball is moving a bit too fast, and it is a bit too red as well. Try to edit examples/mithril/css/ball.css'),
    m('.ball')
  ]
}
